#include "quant.h"

#include <cassert>

void Quant::pseudo_quantize_tensor(tensor* x, tensor* scaling, tensor* zero,
                                   int n_bit, int groupsize) {
  assert()
}

void Quant::write_4bit_tensor(tensor* src, tensor* dst) {}
